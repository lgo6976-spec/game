<style>
.actBadge{display:inline-flex;align-items:center;padding:5px 9px;border-radius:999px;background:var(--soft);border:1px solid var(--line);font-size:11px;font-weight:900}.scanBox{padding:12px;border-radius:15px;background:rgba(88,99,255,.09);border:1px solid rgba(88,99,255,.28);margin-top:10px}.scanBox .title{font-size:14px}.teamModeCard{padding:12px;border-radius:15px;background:var(--soft);margin-bottom:9px;text-align:center}.teamModeCard strong{display:block;font-size:17px}.talkClock{font-size:28px;font-weight:1000;text-align:center;padding:12px;border-radius:15px;background:rgba(255,180,70,.10);border:1px solid rgba(255,180,70,.3)}
</style>
<script>
// v4.4 UI: keep setup simple and let each game adapt internally.
function v44TeamActions(){let d=$('privateActions'),a=S.assignment||{},gd=S.game_data_public||{},mode=gd.team_mode,submitted=gd.my_submitted;if(gd.small_stage==='result'){d.innerHTML='<div class="notice">팀 결과 공개 중입니다.</div>';return}if(submitted){d.innerHTML=`<div class="notice">제출 완료 · ${gd.submitted||0}/${gd.needed||0}</div>`;return}let extra=a.extra||{};if(mode==='sync'){d.innerHTML=`<div class="teamModeCard"><strong>팀 싱크</strong><span>팀원과 상의 없이 같은 답을 노리세요.</span></div>${smallAnswerButtons(extra.options||[])}`;return}if(mode==='number'){d.innerHTML=`<div class="teamModeCard"><strong>팀 숫자</strong><span>팀 평균을 ${extra.target}에 가깝게</span></div><div class="numberGrid">${[1,2,3,4,5,6,7,8,9].map(n=>`<button class="numberPick" onclick="act('submit',{choice:${n}})">${n}</button>`).join('')}</div>`;return}if(mode==='quiz'){d.innerHTML=`<div class="teamModeCard"><strong>${esc(extra.category||'상식')} · 팀 퀴즈</strong><span>팀 전체 정답률로 승부합니다.</span></div>${smallAnswerButtons(extra.options||[])}`;return}if(mode==='chosung'){d.innerHTML=`<div class="teamModeCard"><strong>${esc(extra.category||'초성')} · 팀 초성</strong><span>팀에서 가장 빠른 정답이 중요합니다.</span></div><div class="chosungHero"><div class="initial">${esc(a.mission||'')}</div></div><div class="wordRow"><input id="teamWord" maxlength="24" placeholder="정답 입력" onkeydown="if(event.key==='Enter'){event.preventDefault();submitTeamWord()}"><button class="btn primary" onclick="submitTeamWord()">제출</button></div>`;setTimeout(()=>$('teamWord')?.focus(),30);return}}
function submitTeamWord(){let v=($('teamWord')?.value||'').trim();if(!v)return toast('정답을 입력해 주세요');act('submit',{text:v})}
const _v44SmallActions=renderSmallActions;renderSmallActions=function(){if(S?.game_id==='teamsync')return v44TeamActions();if(S?.game_id==='bluff'&&S?.game_data_public?.small_stage==='bluff_talk'){$('privateActions').innerHTML=`<div class="talkClock">🎭 직접 설득할 시간</div><div class="notice" style="margin-top:8px">휴대폰은 내려놓고 블러퍼의 말을 들어보세요. 곧 진실/거짓 투표가 열립니다.</div>`;$('minorActions').innerHTML='';return}return _v44SmallActions()};

const _v44RenderPlay=renderPlay;renderPlay=function(){_v44RenderPlay();if(!S)return;let gd=S.game_data_public||{};if(S.game_id==='night'){$('roundText').innerHTML=`<span class="actBadge">ACT ${gd.night_act||1}</span>`}if(S.game_id==='infection')renderInfectionScan();if(S.game_id==='chosung'){$('targetLine').textContent=(gd.chosung_level===0?'초성만 공개':gd.chosung_level===1?'분야 공개 · 힌트 전':'힌트 공개됨')+` · ${gd.small_index||1}/${gd.small_total||8}`;}if(S.game_id==='teamsync'){$('roundText').textContent=`팀 대항전 · ${gd.small_index||1}/${gd.small_total||8}${gd.team_final?' · FINAL':''}`}};
function renderInfectionScan(){let gd=S.game_data_public||{},d=$('minorActions');if(!gd.scan_open)return;if(gd.is_infected){d.insertAdjacentHTML('beforeend','<div class="scanBox"><div class="title">감염 검사 진행 중</div><div class="sub">인간들이 검사 대상을 고르고 있습니다. 정체를 숨기세요.</div></div>');return}if(gd.scan_voted){d.insertAdjacentHTML('beforeend',`<div class="scanBox"><div class="title">검사 투표 완료</div><div class="sub">${gd.scan_votes||0}/${gd.scan_humans||0}명</div></div>`);return}let opts=S.players.filter(p=>p.active&&p.id!==S.me.id).map(p=>`<option value="${p.id}">${esc(p.name)}</option>`).join('');d.insertAdjacentHTML('beforeend',`<div class="scanBox"><div class="title">☣️ 인간팀 감염 검사</div><div class="sub" style="margin:5px 0 8px">가장 의심스러운 한 명을 선택하세요. 검사 결과는 완벽하지 않습니다.</div><select id="scanSel">${opts}</select><button class="btn secondary full" style="margin-top:7px" onclick="act('infection_scan',{target:$('scanSel').value})">검사 대상 선택</button></div>`)}

// Anonymous game gets a safe personal pass, while anonymity remains intact.
const _v44RenderActionsBase=renderActions;renderActions=function(){_v44RenderActionsBase();if(S?.game_id==='anon'&&!S.round_result&&!S.game_data_public?.my_submitted){$('minorActions').insertAdjacentHTML('beforeend','<button class="btn ghost" onclick="act(\'anon_pass\')">이번 질문만 패스</button>')}};

// Quiz: only category selection remains visible. Difficulty/count/time are automatic.
const _v44QuizSettings=renderQuizSettings;renderQuizSettings=function(){let p=$('quizSettingsPanel');if(!p)return;if(S?.game_id!=='quiz'){p.classList.add('hidden');return}p.classList.remove('hidden');let cats=S.settings.quiz_categories||QUIZ_CATS;if(isHost())p.innerHTML=`<div class="title">상식 배틀 분야</div><div class="sub">분야만 고르세요. 10문제·제한시간·난이도는 정답률에 맞춰 자동 조절됩니다.</div><div id="quizCatGrid" class="quizCats" style="margin-top:12px">${QUIZ_CATS.map(c=>`<label class="quizCheck"><input type="checkbox" value="${c}" ${cats.includes(c)?'checked':''} onchange="saveV42Settings()"><span>${c}</span></label>`).join('')}</div>`;else p.innerHTML=`<div class="title">상식 배틀</div><div class="sub">${esc(cats.join(' · '))} · 난이도 자동 조절</div>`};

// More game-specific synthetic sound design without adding audio files.
function v44Sfx(name){if(!audioEnabled)return;switch(name){case'hunt':tone(210,.05,'triangle',.025);tone(330,.07,'triangle',.03,.05);break;case'nightAct':tone(95,.38,'sine',.028);tone(142,.28,'triangle',.02,.16);noise(.22,.012,.04);break;case'scan':tone(720,.06,'square',.025);tone(520,.08,'square',.025,.11);tone(320,.12,'sine',.03,.22);break;case'bluff':tone(260,.08,'triangle',.025);tone(310,.08,'triangle',.02,.12);break;case'quizRight':tone(620,.05);tone(880,.09,'sine',.04,.05);break;case'quizReveal':tone(440,.04);tone(554,.04,'sine',.03,.05);tone(659,.09,'sine',.035,.10);break;case'team':tone(330,.06,'square',.02);tone(495,.06,'square',.025,.07);tone(660,.11,'sine',.035,.14);break;case'chosungHint':tone(760,.045,'sine',.025);tone(610,.07,'sine',.025,.06);break;case'betray':tone(260,.08,'sawtooth',.025);tone(120,.18,'sawtooth',.032,.08);break;case'choice':tone(360,.05,'triangle',.02);tone(540,.08,'triangle',.025,.07);break;}}
const _v44HandleAudio=handleAudio;handleAudio=function(oldS,newS){_v44HandleAudio(oldS,newS);if(!oldS||!newS||newS.phase!=='play')return;let ot=(oldS.public?.event_title||'')+' '+(oldS.public?.event_text||''),nt=(newS.public?.event_title||'')+' '+(newS.public?.event_text||'');if(ot===nt)return;let gid=newS.game_id;if(gid==='night'&&/ACT/.test(nt)){v44Sfx('nightAct');vibrate([90,45,120])}else if(gid==='infection'&&/검사/.test(nt)){v44Sfx('scan');vibrate([60,30,60])}else if(gid==='bounty'&&/사냥/.test(nt)){v44Sfx('hunt')}else if(gid==='betray'&&/계약|배신|파기/.test(nt)){v44Sfx('betray');vibrate([80,30,100])}else if(gid==='bluff'){v44Sfx('bluff')}else if(gid==='quiz'&&/정답 공개/.test(nt)){v44Sfx('quizReveal')}else if(gid==='chosung'&&/분야 공개|힌트 공개/.test(nt)){v44Sfx('chosungHint')}else if(gid==='teamsync'&&/팀 결과|팀 대항전/.test(nt)){v44Sfx('team')}else if(gid==='choice'&&/FINAL|선택 공개/.test(nt)){v44Sfx('choice')}};
const _v44SyncBackground=syncBackground;syncBackground=function(){stopBackground();if(!audioEnabled||!S||S.phase!=='play')return;bgStep=0;let gid=S.game_id;let ms=gid==='night'?1700:gid==='forbidden'?2400:gid==='quiz'||gid==='chosung'?1800:gid==='match'?2100:1000;bgTimer=setInterval(()=>{if(!S||S.phase!=='play'||S.paused)return;bgStep++;if(gid==='night'){bgTone(bgStep%2?68:82,1.6,.010);if(bgStep%4===0)bgTone(138,.75,.004,'triangle')}else if(gid==='infection'){bgTone(bgStep%2?88:104,.55,.007,'sawtooth')}else if(gid==='bomb'){/* ticking is handled by heartbeat */}else if(gid==='bounty'){if(bgStep%2===0)bgTone(128,.28,.005,'triangle')}else if(gid==='betray'){bgTone(bgStep%2?112:126,.38,.005,'triangle')}else if(gid==='forbidden'){bgTone(116,.45,.003,'sine')}else if(gid==='quiz'||gid==='chosung'){if(bgStep%2===0)bgTone(220,.16,.003,'triangle')}else if(gid==='teamsync'){if(bgStep%2===0)bgTone(196,.18,.004,'square')}else if(gid==='match'){if(bgStep%2===0)bgTone(246,.22,.003,'sine')}else if(bgStep%2===0)bgTone(165,.3,.003)},ms)};
</script>