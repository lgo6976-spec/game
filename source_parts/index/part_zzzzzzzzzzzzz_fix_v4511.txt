<style>
.mafiaPeople4511{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin:10px 0}.mafiaPerson4511{min-height:58px;border:1px solid var(--line);border-radius:15px;background:var(--soft);color:var(--text);font-size:14px;font-weight:900;padding:10px 8px;transition:transform .08s,border-color .12s,background .12s}.mafiaPerson4511:active{transform:scale(.98)}.mafiaPerson4511.selected{border-color:var(--accent2);background:rgba(98,88,231,.17);box-shadow:0 0 0 1px rgba(98,88,231,.22) inset}.mafiaPerson4511 small{display:block;font-size:10px;color:var(--muted);margin-top:3px}.mafiaConfirm4511{margin-top:8px}.mafiaConfirm4511:disabled{opacity:.45}.mafiaPickHint4511{text-align:center;font-size:11px;color:var(--muted);margin-top:5px}.mafiaSending4511{pointer-events:none;opacity:.7}@media(max-width:360px){.mafiaPeople4511{grid-template-columns:1fr}}</style>
<script>
let _mafiaPick4511={};let _mafiaSending4511=false;
function mafiaPickKey4511(stage){let gd=S?.game_data_public||{};return [S?.room||'',gd.mafia_day||0,stage,S?.assignment?.role||''].join('|')}
function mafiaTargetPeople4511(stage){let gd=S?.game_data_public||{},me=S?.me?.id;if(stage==='final_vote'){let ids=(gd.candidates||[]).filter(id=>id!==me);return ids.map(id=>S.players.find(p=>p.id===id)).filter(p=>p&&p.active&&p.alive!==false)}return (S?.players||[]).filter(p=>p.active&&p.alive!==false&&p.id!==me)}
function mafiaPick4511(stage,index){if(_mafiaSending4511)return;let people=mafiaTargetPeople4511(stage),p=people[index];if(!p)return;_mafiaPick4511[mafiaPickKey4511(stage)]=p.id;updateMafiaPicker4511(stage)}
function mafiaPickAbstain4511(){if(_mafiaSending4511)return;_mafiaPick4511[mafiaPickKey4511('final_vote')]='abstain';updateMafiaPicker4511('final_vote')}
function selectedMafiaTarget4511(stage){return _mafiaPick4511[mafiaPickKey4511(stage)]||null}
function mafiaTargetName4511(id){if(id==='abstain')return '살려둔다';return nameOf(id)||'선택한 사람'}
function mafiaActionInfo4511(stage){let role=S?.assignment?.role||'',gd=S?.game_data_public||{};if(stage==='night'){if(role==='마피아')return {verb:gd.scout_night?'정찰':'제거',action:'mafia_night_action',tone:'primary'};if(role==='경찰')return {verb:'조사',action:'mafia_night_action',tone:'primary'};return {verb:'보호',action:'mafia_night_action',tone:'primary'}}if(stage==='nominate')return {verb:'용의자로 지목',action:'mafia_nominate',tone:'danger'};return {verb:'탈락 후보로 선택',action:'mafia_final_vote',tone:'danger'}}
function mafiaPickerHtml4511(stage){let people=mafiaTargetPeople4511(stage),sel=selectedMafiaTarget4511(stage),info=mafiaActionInfo4511(stage);let buttons=people.map((p,i)=>`<button type="button" class="mafiaPerson4511 ${sel===p.id?'selected':''}" onclick="mafiaPick4511('${stage}',${i})">${esc(p.name)}${sel===p.id?'<small>✓ 선택됨</small>':''}</button>`).join('');if(stage==='final_vote'&&S.game_data_public?.abstain_allowed)buttons+=`<button type="button" class="mafiaPerson4511 ${sel==='abstain'?'selected':''}" onclick="mafiaPickAbstain4511()">살려둔다${sel==='abstain'?'<small>✓ 선택됨</small>':'<small>아직 확신 없음</small>'}</button>`;let label=sel?(sel==='abstain'?'아무도 탈락시키지 않기':`${mafiaTargetName4511(sel)} · ${info.verb}`):'먼저 한 명을 선택하세요';return `<div class="mafiaPeople4511">${buttons}</div><button type="button" id="mafiaConfirm4511" class="btn ${info.tone} full mafiaConfirm4511" ${sel?'':'disabled'} onclick="confirmMafiaPick4511('${stage}')">${esc(label)}</button><div class="mafiaPickHint4511">사람을 탭한 뒤 아래 버튼으로 확정합니다.</div>`}
function updateMafiaPicker4511(stage){let wrap=document.getElementById('mafiaPickerWrap4511');if(wrap)wrap.innerHTML=mafiaPickerHtml4511(stage)}
async function confirmMafiaPick4511(stage){if(_mafiaSending4511)return;let target=selectedMafiaTarget4511(stage);if(!target){toast('먼저 한 명을 선택해 주세요');return}let info=mafiaActionInfo4511(stage),btn=document.getElementById('mafiaConfirm4511');_mafiaSending4511=true;document.getElementById('mafiaPickerWrap4511')?.classList.add('mafiaSending4511');if(btn){btn.disabled=true;btn.textContent='확정 중...'}try{await act(info.action,{target})}finally{_mafiaSending4511=false;document.getElementById('mafiaPickerWrap4511')?.classList.remove('mafiaSending4511')}}
function renderMafiaUI4511(){let d=$('privateActions'),m=$('minorActions'),gd=S.game_data_public||{},stage=gd.mafia_stage,a=S.assignment||{},alive=S.players.find(p=>p.id===S.me.id)?.alive!==false;let sig=JSON.stringify(['mafia4511',stage,gd.mafia_day,gd.my_action,gd.my_nomination,gd.my_final_vote,a.role,alive,gd.candidates,gd.day_evidence,gd.defense_candidate,gd.can_skip_discussion,gd.can_extend_discussion,gd.discussion_extended]);if(d.dataset.sig===sig){if(['night','nominate','final_vote'].includes(stage))updateMafiaPicker4511(stage);return}d.dataset.sig=sig;m.innerHTML='';let summary=gd.role_summary?`<div class="mafiaPublicInfo">역할 구성 · ${esc(gd.role_summary)}</div>`:'';if(!alive){d.innerHTML=`<div class="notice mafiaDead">탈락했습니다. 남은 사람의 토론을 지켜보세요.</div>${summary}`;return}if(stage==='night'){if(!['마피아','경찰','의사'].includes(a.role)){d.innerHTML=`<div class="notice">🌙 밤입니다. 조용히 기다리세요.</div>${summary}`;return}if(gd.my_action){d.innerHTML=`<div class="notice">밤 행동 선택 완료 · 다른 역할을 기다립니다.</div>${summary}`;return}let info=mafiaActionInfo4511('night');d.innerHTML=`<div class="mafiaPhase"><div class="mafiaRole">${esc(a.role)}</div><div class="mafiaHint">${esc(info.verb)}할 사람을 한 명 고르세요.</div>${summary}</div><div id="mafiaPickerWrap4511">${mafiaPickerHtml4511('night')}</div>`;return}if(stage==='morning'){d.innerHTML=`${mafiaEvidenceBlock()}<div class="notice">아침 결과를 확인하고 토론 준비를 하세요.</div>${summary}`;return}if(stage==='discuss'){d.innerHTML=`${mafiaEvidenceBlock()}<div class="mafiaPhase"><div class="mafiaRole">☀️ 토론 시간</div><div class="mafiaHint">단서와 주장의 모순을 찾으세요.</div>${summary}</div>`;if(isHost()){let buttons='';if(gd.can_skip_discussion)buttons+=`<button class="btn ghost" onclick="act('mafia_skip_discussion')">바로 지목하기</button>`;if(gd.can_extend_discussion)buttons+=`<button class="btn secondary" onclick="act('mafia_extend_discussion')">+1분 연장</button>`;if(buttons)m.innerHTML=`<div class="mafiaHostPace456">${buttons}</div>`;else if(gd.discussion_extended)m.innerHTML='<div class="sub">오늘 토론 연장 사용 완료</div>'}return}if(stage==='nominate'){if(gd.my_nomination){d.innerHTML=`${mafiaEvidenceBlock()}<div class="notice">1차 지목 완료 · 다른 생존자를 기다립니다.</div>`;return}d.innerHTML=`${mafiaEvidenceBlock()}<div class="mafiaPhase"><div class="mafiaRole">👁️ 1차 용의자 지목</div><div class="mafiaHint">가장 의심스러운 한 명을 고르세요.</div></div><div id="mafiaPickerWrap4511">${mafiaPickerHtml4511('nominate')}</div>`;return}if(stage==='defense'){let current=gd.defense_candidate,name=current?nameOf(current):mafiaCandidatesText(),mine=current===S.me.id;d.innerHTML=`<div class="mafiaDefenseTurn456"><b>${mine?'지금 당신 차례입니다':esc(name||'후보')+'님의 최후변론'}</b><br>${mine?'15초 동안 핵심 주장과 단서를 정리하세요.':'15초 동안 듣고 판단을 정리하세요.'}</div>${mafiaEvidenceBlock()}`;return}if(stage==='final_vote'){if(gd.my_final_vote){d.innerHTML='<div class="notice">결선투표 완료 · 결과를 기다립니다.</div>';return}d.innerHTML=`${mafiaEvidenceBlock()}<div class="mafiaPhase"><div class="mafiaRole">🗳️ 결선투표</div><div class="mafiaHint">최후변론을 듣고 최종 결정하세요.</div></div><div id="mafiaPickerWrap4511">${mafiaPickerHtml4511('final_vote')}</div>`;return}d.innerHTML='<div class="notice">결과 공개 중입니다.</div>'}
renderMafiaUI=renderMafiaUI4511;
</script>