function toast(t){$('toast').textContent=t;$('toast').classList.add('on');setTimeout(()=>$('toast').classList.remove('on'),1500)}
function view(id){document.querySelectorAll('.view').forEach(x=>x.classList.remove('on'));$(id).classList.add('on');window.scrollTo(0,0);if(!['play','countdown'].includes(id))releaseWakeLock()}
function setConnection(ok){let d=$('roomChip')?.querySelector('.dot');if(!d)return;d.style.background=ok?'var(--ok)':'var(--danger)';if(!ok)$('roomTxt').textContent='재연결 중'}
async function api(path,body,auth=true){let headers=body?{'Content-Type':'application/json'}:{};let me=saved();if(auth&&me?.token)headers['X-Player-Token']=me.token;let r=await fetch(path,{method:body?'POST':'GET',headers,body:body?JSON.stringify(body):undefined});let j=await r.json();if(!r.ok)throw Error(j.error||'오류');return j}
async function createRoom(){if(!audioEnabled)enableSound();try{let j=await api('/api/create',{name:$('createName').value||'방장'},false);setSaved(j.room,j.pid,j.token);await refresh();startPoll()}catch(e){toast(e.message)}}
async function joinRoom(){if(!audioEnabled)enableSound();try{let j=await api('/api/join',{room:$('joinCode').value.trim().toUpperCase(),name:$('joinName').value||'참가자'},false);setSaved(j.room,j.pid,j.token);await refresh();startPoll()}catch(e){toast(e.message)}}
async function act(type,extra={}){let me=saved();if(!me)return;try{await api('/api/action',{room:me.room,pid:me.pid,type,...extra});await refresh()}catch(e){toast(e.message)}}
async function leaveRoom(){let me=saved();if(me){try{await api('/api/action',{room:me.room,pid:me.pid,type:'leave_room'})}catch{}}localStorage.removeItem('bamnolja_me');S=null;clearInterval(polling);stopBackground();releaseWakeLock();$('roomChip').classList.add('hidden');view('home')}
async function copyInvite(){try{await navigator.clipboard.writeText(S.join_url);toast('링크 복사 완료')}catch{toast(S.join_url)}}
async function shareInvite(){try{if(navigator.share)await navigator.share({title:'밤놀자 '+S.room,text:'방 코드 '+S.room,url:S.join_url});else copyInvite()}catch(e){if(e?.name!=='AbortError')copyInvite()}}
async function requestWakeLock(){try{if('wakeLock'in navigator&&document.visibilityState==='visible'&&!wakeLock)wakeLock=await navigator.wakeLock.request('screen')}catch{}}
function releaseWakeLock(){try{wakeLock?.release()}catch{}wakeLock=null}
function startPoll(){clearInterval(polling);polling=setInterval(refresh,850)}
async function refresh(){let me=saved();if(displayMode){if(!displayRoom)return;me={room:displayRoom,pid:'',token:''}}if(!me)return;try{let headers={};if(me.token)headers['X-Player-Token']=me.token;let r=await fetch(`/api/state?room=${encodeURIComponent(me.room)}&pid=${encodeURIComponent(me.pid)}`,{headers});if(!r.ok){if(!displayMode&&[403,404].includes(r.status)){localStorage.removeItem('bamnolja_me');S=null;clearInterval(polling);setConnection(false);view('home');toast(r.status===404?'방이 만료되었습니다. 새 방을 만들어 주세요.':'참가자 인증이 만료되었습니다. 다시 입장해 주세요.');return}throw Error()}let next=await r.json(),old=S;S=next;lastStateAt=Date.now();setConnection(true);handleAudio(old,S);render()}catch{setConnection(false)}}
function localRemaining(key){let v=S?.[key];if(v==null)return 0;if(S?.paused&&key==='remaining')return v;return Math.max(0,v-(Date.now()-lastStateAt)/1000)}
function isHost(){return !!S?.me?.host}
function nameOf(id){return S?.players?.find(p=>p.id===id)?.name||''}
function esc(s){return String(s??'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}
function fmt(sec){sec=Math.max(0,Math.ceil(sec||0));return `${String(Math.floor(sec/60)).padStart(2,'0')}:${String(sec%60).padStart(2,'0')}`}
function render(){if(!S)return;$('roomChip').classList.remove('hidden');$('roomTxt').textContent=`${S.room} · ${S.players.filter(p=>p.active).length}명`;if(displayMode){renderDisplay();return}if(S.phase==='lobby')renderLobby();else if(S.phase==='games')renderGames();else if(S.phase==='guide')renderGuide();else if(S.phase==='countdown')renderCountdown();else if(S.phase==='play')renderPlay();else if(S.phase==='result')renderResult()}

function renderLobby(){view('lobby');$('code').textContent=S.room;$('qr').src=`/api/qr?room=${S.room}&t=${Date.now()}`;$('joinUrl').textContent=S.join_url;$('pcnt').textContent=`${S.players.filter(p=>p.active).length}명`;$('plist').innerHTML=S.players.map(p=>`<span class="player ${p.active?'':'inactive'}">${esc(p.name)}${p.host?' · 방장':''}${isHost()&&!p.host?` <button onclick="act('toggle_active',{target:'${p.id}'})" style="border:0;background:none;color:#9da9bc;padding:0 0 0 4px">${p.active?'제외':'복귀'}</button>`:''}</span>`).join('');$('hostSettings').classList.toggle('hidden',!isHost());$('chooseBtn').classList.toggle('hidden',!isHost());$('displayBtn').classList.toggle('hidden',!isHost());if(isHost()){$('tempo').value=S.settings.tempo||'brisk';$('spice').value=S.settings.spice||'mild';$('touch').checked=!!S.settings.touch;$('drink').checked=!!S.settings.drink;$('dating').checked=!!S.settings.dating;$('verify').checked=S.settings.verification!==false}}
function saveSettings(){act('settings',{tempo:$('tempo').value,spice:$('spice').value,touch:$('touch').checked,drink:$('drink').checked,dating:$('dating').checked,verification:$('verify').checked})}
function paceTag(g){if(['bomb','oddq','anon','choice'].includes(g.id))return '빠름';if(['night','forbidden'].includes(g.id))return '길게';return '템포형'}
function renderGames(){view('games');$('gamesGrid').innerHTML=S.games.map(g=>`<div class="game"><div class="gameTop"><div><div class="rank">추천 ${g.rank}위</div><div class="gameName">${esc(g.title)}</div></div><span class="tag ${paceTag(g)==='빠름'?'hot':''}">${paceTag(g)}</span></div><p>${esc(g.desc)}</p><div class="tags"><span class="tag">${esc(g.players)}</span><span class="tag">${esc(g.time)}</span><span class="tag">${esc(g.cat)}</span></div>${isHost()?`<button class="btn ${g.rank<=3?'primary':'secondary'} full" style="margin-top:11px" onclick="act('select_game',{game_id:'${g.id}'})">이 게임</button>`:`<div class="sub" style="margin-top:11px">방장이 고르는 중</div>`}</div>`).join('')}
function renderGuide(){view('guide');let g=S.game;$('gCat').textContent=g.cat;$('gTitle').textContent=g.title;$('gMeta').textContent=`${g.players} · ${g.time}`;$('gDesc').textContent=g.desc;$('gSteps').innerHTML=g.guide.slice(0,4).map(x=>`<div class="step">${esc(x)}</div>`).join('');$('startBtn').classList.toggle('hidden',!isHost());$('guideBack').classList.toggle('hidden',!isHost());let st=S.public.story;$('storyBox').classList.toggle('hidden',!st);if(st)$('storyBox').innerHTML=`<b>${esc(st.title)}</b><br>${esc(st.hook)}`}
function renderCountdown(){view('countdown');requestWakeLock();updateLiveTimers()}
function renderPlay(){view('play');requestWakeLock();let g=S.game,a=S.assignment||{};$('playTitle').textContent=g.title;$('roundText').textContent=['oddq','anon','choice'].includes(S.game_id)?`${S.round_no}/3 라운드`:'진행 중';$('role').textContent=a.role||'플레이어';$('mission').textContent=a.mission||'잠시 기다리세요.';$('targetLine').textContent=a.target?`대상 · ${nameOf(a.target)}`:'';let pn=a.extra?.private_notice||'';$('privateNotice').classList.toggle('hidden',!pn);$('privateNotice').textContent=pn;$('eventTitle').textContent=S.public.event_title||'SYSTEM';$('eventText').textContent=S.public.event_text||'';renderPending();renderActions();renderStats();renderRoundResult();$('hostControls').classList.toggle('hidden',!isHost());$('pauseBtn').textContent=S.paused?'재개':'일시정지';$('nightFinish').classList.toggle('hidden',!(isHost()&&S.game_id==='night'));let autoRound=['oddq','anon','choice'].includes(S.game_id);$('nextRoundBtn').classList.toggle('hidden',!(isHost()&&autoRound&&S.round_result));updateWaiting();updateLiveTimers()}
function renderPending(){let p=S.pending?.[0];pendingId=p?.id||null;$('pendingBox').classList.toggle('hidden',!p);if(p)$('pendingText').textContent=`${p.from}님이 “${p.detail}” 성공을 신고했습니다. 실제로 성공했나요?`}
function confirmPending(ok){if(pendingId)act('confirm',{id:pendingId,ok})}
function peopleSelect(id){return `<select id="${id}">${S.players.filter(p=>p.active&&p.id!==S.me.id).map(p=>`<option value="${p.id}">${esc(p.name)}</option>`).join('')}</select>`}
function canReroll(){return ['bounty','night','secret','bomb','infection','forbidden'].includes(S.game_id)&&(S.assignment?.extra?.rerolls_left||0)>0}
function renderActions(){let d=$('privateActions'),m=$('minorActions'),gid=S.game_id,a=S.assignment||{},stage=S.round_result?.stage||'none',sig=JSON.stringify([gid,S.round_no,a.role,a.mission,a.target,stage,!!S.round_result]);if(d.dataset.sig===sig)return;d.dataset.sig=sig;let h='';
 if(['bounty','bomb','infection','forbidden','secret','night'].includes(gid))h=`<button class="btn primary full" onclick="act('mission_success')">성공 신고</button>`;
 if(gid==='oddq'){if(!S.round_result)h=`${peopleSelect('personSel')}<textarea id="reason" maxlength="80" placeholder="이유는 짧게 · 선택사항"></textarea><button class="btn primary full" onclick="act('submit',{target:$('personSel').value,reason:$('reason').value})">선택 완료</button>`;else if(S.round_result.stage==='guess')h=`${peopleSelect('guessSel')}<button class="btn primary full" onclick="act('guess',{target:$('guessSel').value})">이 사람이 다른 질문</button>`;else h=`<div class="notice">정답 공개 중 · 자동으로 다음 라운드로 넘어갑니다.</div>`}
 if(gid==='anon'){if(!S.round_result)h=`${peopleSelect('personSel')}<textarea id="reason" maxlength="80" placeholder="이유 한 줄 · 선택사항"></textarea><button class="btn primary full" onclick="act('submit',{target:$('personSel').value,reason:$('reason').value})">익명 제출</button>`;else h=`<div class="notice">결과 공개 중 · 곧 다음 라운드가 시작됩니다.</div>`}
 if(gid==='choice'&&a.extra){if(!S.round_result)h=`<div class="stack"><button class="btn primary full" onclick="act('submit',{choice:'a'})">${esc(a.extra.a)}</button><button class="btn secondary full" onclick="act('submit',{choice:'b'})">${esc(a.extra.b)}</button></div>`;else h=`<div class="notice">선택 결과 공개 중 · 곧 다음 라운드가 시작됩니다.</div>`}
 if(gid==='betray')h=`<button class="btn primary full" onclick="act('mission_success')">목표 진행 +2</button><div style="height:7px"></div>${peopleSelect('allySel')}<div class="row" style="margin-top:7px"><button class="btn secondary" onclick="act('alliance',{target:$('allySel').value})">동맹</button><button class="btn danger" onclick="act('betray')">배신 +3</button></div>`;
 if(gid==='night'&&a.role==='조사자'&&!a.extra?.investigation_used)h+=`<div class="divider"></div><div class="sub">조사자 1회 능력</div>${peopleSelect('investigateSel')}<button class="btn secondary full" style="margin-top:7px" onclick="act('night_investigate',{target:$('investigateSel').value})">비밀 조사</button>`;
 if(gid==='bounty'&&a.role==='👻 방해자'&&!a.extra?.ghost_used){let live=S.players.filter(p=>p.active&&p.alive&&p.id!==S.me.id);h=`<div class="notice">탈락했지만 계속 참여합니다. 가짜 경고를 한 번 보낼 수 있습니다.</div><select id="ghostSel">${live.map(p=>`<option value="${p.id}">${esc(p.name)}</option>`).join('')}</select><button class="btn secondary full" style="margin-top:7px" onclick="act('ghost_hint',{target:$('ghostSel').value})">가짜 경고 보내기</button>`}
 if(gid==='night')h+=`<div class="divider"></div>${peopleSelect('nightVote')}<button class="btn ghost full" style="margin-top:7px" onclick="act('night_vote',{target:$('nightVote').value})">현재 의심 투표</button>`;
 d.innerHTML=h;m.innerHTML=canReroll()?`<button class="btn ghost" onclick="act('reroll')">미션 바꾸기 · 1회</button>`:'';
}
function updateWaiting(){let box=$('waitingInfo'),gd=S.game_data_public||{},gid=S.game_id;if(!['oddq','anon','choice'].includes(gid)){box.classList.add('hidden');return}let n=gd.submitted||0,total=gd.needed||0;if(S.round_result?.stage==='guess'){n=gd.guessed||0;total=gd.needed||0}box.classList.remove('hidden');box.innerHTML=`<span>${S.round_result?.stage==='guess'?'추리 제출':'선택 완료'}</span><strong>${n}/${total}</strong>`}
function renderStats(){let extra='';if(S.game_id==='infection')extra=`<div class="scoreline"><span>감염률</span><b>${S.game_data_public.infection_rate||0}%</b></div>`;$('stats').innerHTML=extra+[...S.players].filter(p=>p.active).sort((a,b)=>b.score-a.score).map(p=>`<div class="scoreline"><span>${esc(p.name)}${!p.alive?' · 방해자':''}</span><span class="pill">${p.score}점</span></div>`).join('')}
function renderRoundResult(){let r=S.round_result;$('roundResultBox').classList.toggle('hidden',!r);if(!r)return;let h='';if(r.text)h+=`<div class="notice">${esc(r.text)}</div>`;if(r.counts){if(Array.isArray(r.counts))h+=`<div class="score" style="margin-top:7px">${r.counts.map(x=>`<div class="scoreline"><span>${esc(x.name)}</span><b>${x.count}표</b></div>`).join('')}</div>`;else h+=`<div class="notice">A ${r.counts.a}명 · B ${r.counts.b}명</div>`}if(r.answers)h+=`<div class="stack" style="margin-top:7px">${r.answers.map(x=>`<div class="notice"><b>${esc(x.player)}</b> → ${esc(x.target)}${x.reason?`<br>${esc(x.reason)}`:''}</div>`).join('')}</div>`;if(r.reasons?.length)h+=`<div class="stack" style="margin-top:7px">${r.reasons.map(x=>`<div class="notice">“${esc(x.reason)}” → ${esc(x.target)}</div>`).join('')}</div>`;if(r.odd_name)h+=`<div class="notice" style="margin-top:7px"><b>다른 질문:</b> ${esc(r.odd_name)}</div>`;$('roundResult').innerHTML=h}
function renderResult(){view('result');$('winner').textContent=S.result?.winner||'공동 결과';$('resultTitle').textContent=(S.result?.title||'')+' · 이번 판';let gp=S.result?.game_points||{};$('resultScores').innerHTML=[...S.players].filter(p=>p.active).sort((a,b)=>(gp[b.id]||0)-(gp[a.id]||0)).map((p,i)=>`<div class="scoreline"><span>${i+1}. ${esc(p.name)} <span class="sub">· 누적 ${p.score}</span></span><b>${(gp[p.id]||0)>=0?'+':''}${gp[p.id]||0}</b></div>`).join('');$('replayBtn').classList.toggle('hidden',!isHost());$('otherBtn').classList.toggle('hidden',!isHost())}
function showToday(){if(!S)return;returnView=S.phase;view('today');let t=S.today;$('todayText').innerHTML=`${t.games||0}게임 플레이<br><b>종합 1위 ${esc(t.leader||'-')}</b> · ${t.leader_score||0}점<br>최다 제거 ${esc(t.hunter||'-')} · ${t.hunter_n||0}회<br>미션왕 ${esc(t.missions||'-')} · ${t.missions_n||0}회<br>배신왕 ${esc(t.betrayer||'-')} · ${t.betrayals||0}회`;$('todayScores').innerHTML=[...S.players].filter(p=>p.active).sort((a,b)=>b.score-a.score).map((p,i)=>`<div class="scoreline"><span>${i+1}. ${esc(p.name)}</span><b>${p.score}</b></div>`).join('')}
function goCurrent(){render()}
function openDisplay(){window.open(`/?room=${S.room}&display=1`,'_blank')}
function renderDisplay(){view('display');$('roomChip').classList.add('hidden');$('dRoom').textContent=S.room;$('dTitle').textContent=S.game?.title||'밤놀자';$('dSub').textContent=S.phase==='lobby'?'QR로 참가하세요.':(S.public.story?.hook||'비밀정보는 참가자 휴대폰에만 표시됩니다.');$('dEventTitle').textContent=S.public.event_title||'SYSTEM';$('dEventText').textContent=S.public.event_text||'';$('dScores').innerHTML=[...S.players].filter(p=>p.active).sort((a,b)=>b.score-a.score).map((p,i)=>`<div class="scoreline"><span>${i+1}. ${esc(p.name)}</span><b>${p.score}</b></div>`).join('');updateLiveTimers()}
function updateLiveTimers(){if(!S)return;let rem=localRemaining('remaining'),ir=localRemaining('interaction_remaining');if($('timer'))$('timer').textContent=fmt(rem);if($('countNum')&&S.phase==='countdown'){$('countNum').textContent=Math.max(1,Math.ceil(rem));let n=Math.ceil(rem);if(n!==lastCountdownSecond){lastCountdownSecond=n;sfx('count');vibrate(35)}}if($('dTimer')&&displayMode)$('dTimer').textContent=S.phase==='play'?fmt(rem):S.phase==='countdown'?Math.max(1,Math.ceil(rem)):'--:--';let bar=$('interactionBar'),fill=$('interactionFill');if(bar&&S.phase==='play'){bar.classList.toggle('hidden',!S.interaction_remaining);if(S.interaction_remaining){let max=35,p=Math.max(0,Math.min(100,ir/max*100));fill.style.width=p+'%'}}}
setInterval(updateLiveTimers,250);
document.addEventListener('visibilitychange',()=>{if(!document.hidden){if(audioEnabled&&audioCtx?.state==='suspended')audioCtx.resume().catch(()=>{});refresh();if(S&&['countdown','play'].includes(S.phase))requestWakeLock()}else wakeLock=null});
window.addEventListener('online',refresh);window.addEventListener('offline',()=>setConnection(false));
if(qp.get('room'))$('joinCode').value=qp.get('room').toUpperCase();
let me=saved();if(displayMode){refresh().then(startPoll)}else if(me){refresh().then(startPoll)}
</script>
</body></html>