<style>
:root{color-scheme:dark}
html[data-theme="light"]{color-scheme:light;--bg:#f5f7fb;--panel:#ffffff;--panel2:#ffffff;--soft:#eef1f6;--line:#dce2eb;--text:#17202d;--muted:#667386;--accent:#f05f59;--accent2:#6258e7;--ok:#168b68;--warn:#9a6700;--danger:#d94451;--shadow:0 12px 34px rgba(40,53,73,.10)}
html[data-theme="light"] body{background:radial-gradient(circle at 12% -10%,rgba(98,88,231,.09),transparent 30%),radial-gradient(circle at 105% 0,rgba(240,95,89,.07),transparent 30%),var(--bg);color:var(--text)}
html[data-theme="light"] .top{background:linear-gradient(180deg,rgba(245,247,251,.98),rgba(245,247,251,.88),transparent)}
html[data-theme="light"] .card,html[data-theme="light"] .game,html[data-theme="light"] .nowCard{background:linear-gradient(160deg,#fff,#f9fafc);border-color:var(--line)}
html[data-theme="light"] .chip,html[data-theme="light"] .iconbtn,html[data-theme="light"] input,html[data-theme="light"] select,html[data-theme="light"] textarea,html[data-theme="light"] .notice,html[data-theme="light"] .step,html[data-theme="light"] .scoreline,html[data-theme="light"] .waiting{background:#fff;color:var(--text);border-color:#d5dce7}
html[data-theme="light"] .secondary,html[data-theme="light"] .player,html[data-theme="light"] .pill{background:#e9edf4;color:#18202c;border-color:#d3d9e3}
html[data-theme="light"] .ghost{color:#344055;border-color:#cbd3df}html[data-theme="light"] .danger{background:#fff0f1;color:#b72e3c;border-color:#efc4c9}
html[data-theme="light"] .quickRule{background:#f2f1ff;color:#20203b;border-color:#d8d5f5}html[data-theme="light"] .eventTicker{background:#f7f7ff;border-color:#deddf0}html[data-theme="light"] .eventTicker div{color:#394459}
html[data-theme="light"] .bottom{background:linear-gradient(transparent,rgba(245,247,251,.98) 38%)}html[data-theme="light"] .sub,html[data-theme="light"] .game p{color:var(--muted)}
.quizCats{display:flex;flex-wrap:wrap;gap:7px}.quizCheck{position:relative}.quizCheck input{position:absolute;opacity:0;pointer-events:none}.quizCheck span{display:block;padding:8px 10px;border-radius:999px;border:1px solid var(--line);background:var(--soft);font-size:11px;font-weight:850;cursor:pointer}.quizCheck input:checked+span{background:var(--accent2);border-color:var(--accent2);color:#fff}
.quizOptions{display:grid;grid-template-columns:1fr 1fr;gap:8px}@media(max-width:520px){.quizOptions{grid-template-columns:1fr}}
.quizOption{min-height:60px;border-radius:15px;border:1px solid #3a4558;background:#181e29;color:#fff;text-align:left;padding:11px 12px;display:flex;gap:10px;align-items:center}.quizOption span{width:28px;height:28px;border-radius:9px;display:grid;place-items:center;background:#2b3445;font-weight:950}.quizOption b{font-size:14px}.quizOption:active{transform:scale(.985)}
html[data-theme="light"] .quizOption{background:#fff;color:#17202d;border-color:#d3dae5}html[data-theme="light"] .quizOption span{background:#edf0f6}
.quizCorrect{display:flex;justify-content:space-between;align-items:center;gap:12px;padding:12px;border-radius:14px;background:rgba(57,207,157,.10);border:1px solid rgba(57,207,157,.35)}.quizCorrect span{font-size:11px;font-weight:850}.quizCorrect strong{font-size:19px}.answerReveal{display:grid;gap:6px}.answerPerson{display:flex;justify-content:space-between;gap:10px;padding:9px 10px;border-radius:11px;background:var(--soft);font-size:12px}.answerPerson.isCorrect{border-left:3px solid var(--ok)}.answerPerson.isWrong{border-left:3px solid var(--danger)}.revealTitle{font-size:11px;font-weight:900;color:var(--muted);margin:12px 0 6px}.penaltyCard{margin-top:12px;padding:14px;border-radius:16px;background:rgba(255,103,109,.10);border:1px solid rgba(255,103,109,.35)}.penaltyCard strong{display:block;font-size:22px;margin:4px 0}.penaltyCard div:last-child{font-size:14px}
</style>
<script>
const THEME_KEY='bamnolja_theme';
function applyTheme(mode){let m=mode||localStorage.getItem(THEME_KEY)||'dark';document.documentElement.dataset.theme=m;localStorage.setItem(THEME_KEY,m);let b=$('themeToggle');if(b){b.textContent=m==='light'?'☀️':'🌙';b.title=m==='light'?'라이트 모드':'다크 모드'}}
function toggleTheme(){applyTheme(document.documentElement.dataset.theme==='light'?'dark':'light')}
(function(){let top=document.querySelector('.topright');if(top&&!$('themeToggle')){let b=document.createElement('button');b.id='themeToggle';b.className='iconbtn';b.onclick=toggleTheme;top.insertBefore(b,$('soundUnlock'));}applyTheme();})();
const QUIZ_CATS=['한국사','세계사','인물','동물','과학','지리','스포츠','문화','생활','우주'];
function ensureV42UI(){let hs=$('hostSettings');if(hs&&!$('penaltySettings')){let d=document.createElement('details');d.id='penaltySettings';d.innerHTML=`<summary>오늘의 벌칙</summary><div class="settingsGrid" style="margin-top:10px"><div><div class="label">벌칙 모드</div><select id="penaltyMode" onchange="saveV42Settings()"><option value="off">사용 안 함</option><option value="final">오늘 마지막에 한 번</option><option value="per_game">게임마다</option></select></div><div><div class="label">대상</div><select id="penaltyTarget" onchange="saveV42Settings()"><option value="last1">최종 꼴찌 1명</option><option value="bottom2">하위 2명</option><option value="random_bottom3">하위 3명 중 랜덤 1명</option></select></div><div><div class="label">벌칙 내용</div><select id="penaltySource" onchange="saveV42Settings()"><option value="default">기본 벌칙 랜덤</option><option value="custom">직접 입력만</option><option value="mixed">기본 + 직접 입력</option></select></div><div><div class="label">직접 입력 · 한 줄에 하나</div><textarea id="customPenalties" maxlength="720" placeholder="아침 커피 사기&#10;설거지 담당&#10;편의점 심부름" onblur="saveV42Settings()"></textarea></div></div><div class="sub" style="margin-top:8px">게임별 규칙과 벌칙은 분리됩니다. ‘오늘 마지막’은 종합 포인트가 낮은 사람을 기준으로 뽑습니다.</div>`;hs.appendChild(d)}let sb=$('storyBox');if(sb&&!$('quizSettingsPanel')){let p=document.createElement('div');p.id='quizSettingsPanel';p.className='card hidden';p.style.marginTop='10px';sb.insertAdjacentElement('afterend',p)}if($('today')&&!$('penaltyRevealBox')){let card=$('today').querySelector('.card');let p=document.createElement('div');p.id='penaltyRevealBox';p.className='notice hidden';p.style.marginTop='12px';card.appendChild(p);let b=document.createElement('button');b.id='revealPenaltyBtn';b.className='btn danger full hidden';b.style.marginTop='10px';b.textContent='오늘의 벌칙 공개';b.onclick=()=>act('reveal_penalty');card.appendChild(b)}if($('result')&&!$('resultExtra')){let card=$('result').querySelector('.card');let p=document.createElement('div');p.id='resultExtra';p.style.marginTop='12px';card.appendChild(p)}}
ensureV42UI();
function saveV42Settings(){if(!S||!isHost())return;let cats=[...document.querySelectorAll('#quizCatGrid input:checked')].map(x=>x.value);let custom=($('customPenalties')?.value||'').split(/\n+/).map(x=>x.trim()).filter(Boolean);act('settings',{quiz_categories:cats.length?cats:QUIZ_CATS,quiz_diff:$('quizDiff')?.value||S.settings.quiz_diff||'all',quiz_count:Number($('quizCount')?.value||S.settings.quiz_count||10),quiz_seconds:Number($('quizSeconds')?.value||S.settings.quiz_seconds||8),penalty_mode:$('penaltyMode')?.value||S.settings.penalty_mode||'off',penalty_target:$('penaltyTarget')?.value||S.settings.penalty_target||'last1',penalty_source:$('penaltySource')?.value||S.settings.penalty_source||'default',custom_penalties:custom})}
function syncPenaltySettings(){if(!$('penaltyMode')||!S?.settings)return;$('penaltyMode').value=S.settings.penalty_mode||'off';$('penaltyTarget').value=S.settings.penalty_target||'last1';$('penaltySource').value=S.settings.penalty_source||'default';let ta=$('customPenalties');if(document.activeElement!==ta)ta.value=(S.settings.custom_penalties||[]).join('\n')}
function renderQuizSettings(){let p=$('quizSettingsPanel');if(!p)return;if(S?.game_id!=='quiz'){p.classList.add('hidden');return}p.classList.remove('hidden');let cats=S.settings.quiz_categories||QUIZ_CATS;if(isHost())p.innerHTML=`<div class="title">상식 배틀 설정</div><div class="sub">분야는 여러 개 선택할 수 있습니다. 문제는 최근 출제분을 최대한 피합니다.</div><div id="quizCatGrid" class="quizCats" style="margin-top:12px">${QUIZ_CATS.map(c=>`<label class="quizCheck"><input type="checkbox" value="${c}" ${cats.includes(c)?'checked':''} onchange="saveV42Settings()"><span>${c}</span></label>`).join('')}</div><div class="settingsGrid" style="margin-top:12px"><div><div class="label">난이도</div><select id="quizDiff" onchange="saveV42Settings()"><option value="all">랜덤</option><option value="easy">쉬움</option><option value="normal">보통</option><option value="hard">어려움</option></select></div><div><div class="label">문제 수</div><select id="quizCount" onchange="saveV42Settings()"><option>5</option><option>10</option><option>15</option><option>20</option></select></div><div><div class="label">문제당 제한시간</div><select id="quizSeconds" onchange="saveV42Settings()"><option value="5">5초</option><option value="8">8초</option><option value="12">12초</option><option value="15">15초</option></select></div></div><div class="sub" style="margin-top:8px">정답 +2점 · 가장 빠른 정답자 추가 +1점 · 정답 공개 때 전원의 선택 공개</div>`;else p.innerHTML=`<div class="title">상식 배틀</div><div class="sub">${esc(cats.join(' · '))} · 방장이 문제 구성을 정하는 중입니다.</div>`;if(isHost()){$('quizDiff').value=S.settings.quiz_diff||'all';$('quizCount').value=String(S.settings.quiz_count||10);$('quizSeconds').value=String(S.settings.quiz_seconds||8)}}
const _v42RenderLobby=renderLobby;renderLobby=function(){_v42RenderLobby();ensureV42UI();syncPenaltySettings()};
const _v42RenderGuide=renderGuide;renderGuide=function(){_v42RenderGuide();ensureV42UI();renderQuizSettings()};
const _v42RenderActions=renderActions;renderActions=function(){if(S?.game_id!=='quiz')return _v42RenderActions();let d=$('privateActions'),m=$('minorActions'),a=S.assignment||{},submitted=S.game_data_public?.my_submitted,stage=S.game_data_public?.quiz_stage;let sig=JSON.stringify([S.round_no,a.mission,a.extra?.options,submitted,stage]);if(d.dataset.sig===sig)return;d.dataset.sig=sig;m.innerHTML='';if(stage==='answer'&&!submitted)d.innerHTML=`<div class="quizOptions">${(a.extra?.options||[]).map((o,i)=>`<button class="quizOption" onclick="act('submit',{choice:${JSON.stringify(o)}})"><span>${String.fromCharCode(65+i)}</span><b>${esc(o)}</b></button>`).join('')}</div>`;else if(stage==='answer')d.innerHTML=`<div class="notice">선택 완료 · ${S.game_data_public.submitted}/${S.game_data_public.needed}명 응답</div>`;else d.innerHTML='<div class="notice">정답과 친구들의 선택을 공개하는 중입니다.</div>'};
const _v42RenderPlay=renderPlay;renderPlay=function(){_v42RenderPlay();if(S?.game_id==='quiz'){$('roundText').textContent=`문제 ${S.game_data_public.quiz_index}/${S.game_data_public.quiz_total}`;$('role').textContent=S.assignment?.extra?.category||'상식';$('targetLine').textContent='';$('eventTitle').textContent=S.public.event_title||'상식 배틀'}};
const _v42UpdateLiveTimers=updateLiveTimers;updateLiveTimers=function(){_v42UpdateLiveTimers();if(S?.game_id==='quiz'&&S.phase==='play'){let ir=localRemaining('interaction_remaining'),max=S.game_data_public?.interaction_total||8,fill=$('interactionFill');if(fill)fill.style.width=Math.max(0,Math.min(100,ir/max*100))+'%'}};
const _v42RenderRoundResult=renderRoundResult;renderRoundResult=function(){let r=S?.round_result;if(S?.game_id==='quiz'){$('roundResultBox').classList.toggle('hidden',!r);if(!r)return;let h=`<div class="quizCorrect"><span>정답</span><strong>${esc(r.correct_answer)}</strong></div><div class="sub" style="margin:8px 0 10px">${esc(r.explanation||'')}</div><div class="answerReveal">${(r.answers||[]).map(x=>`<div class="answerPerson ${x.correct?'isCorrect':'isWrong'}"><span>${x.correct?'✓':'✕'} ${esc(x.player)}${x.fastest?' · ⚡최속':''}</span><b>${esc(x.choice)}</b></div>`).join('')}</div>`;$('roundResult').innerHTML=h;return}_v42RenderRoundResult();r=S?.round_result;if(!r)return;let extra='';if(r.guesses?.length)extra+=`<div class="revealTitle">최종 추리 공개</div>${r.guesses.map(x=>`<div class="answerPerson ${x.correct?'isCorrect':'isWrong'}"><span>${x.correct?'✓':'✕'} ${esc(x.player)}</span><b>${esc(x.choice)}</b></div>`).join('')}`;if(r.selections?.length)extra+=`<div class="revealTitle">누가 무엇을 골랐나</div>${r.selections.map(x=>`<div class="answerPerson"><span>${esc(x.player)}</span><b>${esc(x.choice)}</b></div>`).join('')}`;if(r.votes?.length)extra+=`<div class="revealTitle">최종 투표 공개</div>${r.votes.map(x=>`<div class="answerPerson"><span>${esc(x.player)}</span><b>→ ${esc(x.choice)}</b></div>`).join('')}`;if(extra)$('roundResult').insertAdjacentHTML('beforeend',extra)};
const _v42RenderResult=renderResult;renderResult=function(){_v42RenderResult();ensureV42UI();let box=$('resultExtra'),pp=S?.result?.party_points||{};let rows=S.players.filter(p=>p.active).sort((a,b)=>(pp[b.id]||0)-(pp[a.id]||0)).map(p=>`<div class="scoreline"><span>${esc(p.name)}</span><b>오늘 +${pp[p.id]||0}</b></div>`).join('');let reveal='';if(S.penalty?.kind==='game')reveal=`<div class="penaltyCard"><div class="eyebrow">PENALTY</div><strong>${esc((S.penalty.targets||[]).join(' · '))}</strong><div>${esc(S.penalty.text||'')}</div></div>`;box.innerHTML=`<div class="divider"></div><div class="label">오늘 종합 포인트 환산</div><div class="score">${rows}</div>${reveal}`};
const _v42RenderTodayData=renderTodayData;renderTodayData=function(){_v42RenderTodayData();ensureV42UI();$('todayScores').innerHTML=[...S.players].filter(p=>p.active).sort((a,b)=>(b.party_points||0)-(a.party_points||0)).map((p,i)=>`<div class="scoreline"><span>${i+1}. ${esc(p.name)} <span class="sub">· 게임점수 ${p.score}</span></span><b>${p.party_points||0}P</b></div>`).join('');let box=$('penaltyRevealBox'),btn=$('revealPenaltyBtn'),mode=S.settings?.penalty_mode||'off';btn.classList.toggle('hidden',!(isHost()&&mode==='final'));if(S.penalty?.kind==='final'){box.classList.remove('hidden');box.innerHTML=`<div class="eyebrow">FINAL PENALTY</div><div style="font-size:22px;font-weight:950;margin:5px 0">${esc((S.penalty.targets||[]).join(' · '))}</div><div>${esc(S.penalty.text||'')}</div>`}else box.classList.add('hidden')};
if(S){ensureV42UI();syncPenaltySettings()}
</script>
