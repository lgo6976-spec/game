<style>html,body{overscroll-behavior-y:none!important}</style>
<script>
let manualView=null,refreshInFlight=null;
function localRemaining(key){let v=S?.[key];if(v==null)return 0;if(S?.paused)return v;return Math.max(0,v-(Date.now()-lastStateAt)/1000)}
function view(id){let cur=document.querySelector('.view.on');if(cur?.id===id)return;document.querySelectorAll('.view').forEach(x=>x.classList.remove('on'));$(id).classList.add('on');window.scrollTo({top:0,left:0,behavior:'auto'});if(!['play','countdown'].includes(id))releaseWakeLock()}
function startPoll(){clearInterval(polling);polling=setInterval(()=>refresh(false),1200)}
async function refresh(force=false){if(refreshInFlight){if(!force)return refreshInFlight;try{await refreshInFlight}catch{}}let job=(async()=>{let me=saved();if(displayMode){if(!displayRoom)return;me={room:displayRoom,pid:'',token:''}}if(!me)return;try{let headers={};if(me.token)headers['X-Player-Token']=me.token;let r=await fetch(`/api/state?room=${encodeURIComponent(me.room)}&pid=${encodeURIComponent(me.pid)}`,{headers,cache:'no-store'});if(!r.ok){if(!displayMode&&[403,404].includes(r.status)){localStorage.removeItem('bamnolja_me');S=null;clearInterval(polling);setConnection(false);manualView=null;view('home');toast(r.status===404?'방이 만료되었습니다. 새 방을 만들어 주세요.':'참가자 인증이 만료되었습니다. 다시 입장해 주세요.');return}throw Error()}let next=await r.json(),old=S;S=next;lastStateAt=Date.now();setConnection(true);handleAudio(old,S);render()}catch{setConnection(false)}})();refreshInFlight=job;try{return await job}finally{if(refreshInFlight===job)refreshInFlight=null}}
async function act(type,extra={}){let me=saved();if(!me)return;try{await api('/api/action',{room:me.room,pid:me.pid,type,...extra});await refresh(true)}catch(e){toast(e.message)}}
function render(){if(!S)return;$('roomChip').classList.remove('hidden');$('roomTxt').textContent=`${S.room} · ${S.players.filter(p=>p.active).length}명`;if(displayMode){renderDisplay();return}if(manualView==='today'){if(S.phase===returnView){renderTodayData();return}else{manualView=null;returnView=null}}if(S.phase==='lobby')renderLobby();else if(S.phase==='games')renderGames();else if(S.phase==='guide')renderGuide();else if(S.phase==='countdown')renderCountdown();else if(S.phase==='play')renderPlay();else if(S.phase==='result')renderResult()}
function renderLobby(){view('lobby');$('code').textContent=S.room;if($('qr').dataset.room!==S.room){$('qr').dataset.room=S.room;$('qr').src=`/api/qr?room=${encodeURIComponent(S.room)}`}$('joinUrl').textContent=S.join_url;$('pcnt').textContent=`${S.players.filter(p=>p.active).length}명`;$('plist').innerHTML=S.players.map(p=>`<span class="player ${p.active?'':'inactive'}">${esc(p.name)}${p.host?' · 방장':''}${isHost()&&!p.host?` <button onclick="act('toggle_active',{target:'${p.id}'})" style="border:0;background:none;color:#9da9bc;padding:0 0 0 4px">${p.active?'제외':'복귀'}</button>`:''}</span>`).join('');$('hostSettings').classList.toggle('hidden',!isHost());$('chooseBtn').classList.toggle('hidden',!isHost());$('displayBtn').classList.toggle('hidden',!isHost());if(isHost()){$('tempo').value=S.settings.tempo||'brisk';$('spice').value=S.settings.spice||'mild';$('touch').checked=!!S.settings.touch;$('drink').checked=!!S.settings.drink;$('dating').checked=!!S.settings.dating;$('verify').checked=S.settings.verification!==false}}
function canReroll(){let g=S.game_id,a=S.assignment||{},r=a.role||'',me=S.me&&S.players.find(p=>p.id===S.me.id);if(!me?.active||(a.extra?.rerolls_left||0)<=0)return false;if(g==='secret'||g==='night')return true;if(g==='bounty')return r==='사냥꾼'&&!!a.target;if(g==='bomb')return r==='💣 폭탄 보유'&&!!a.target;if(g==='infection')return r==='☣️ 감염자'&&!!a.target;if(g==='forbidden')return r==='유도자'&&!!a.target;return false}
function canReportSuccess(){let g=S.game_id,a=S.assignment||{},r=a.role||'',me=S.me&&S.players.find(p=>p.id===S.me.id);if(!me?.active)return false;if(g==='secret'||g==='night'||g==='betray')return true;if(g==='bounty')return r==='사냥꾼'&&!!a.target&&S.me&&S.players.find(p=>p.id===S.me.id)?.alive;if(g==='bomb')return r==='💣 폭탄 보유'&&!!a.target;if(g==='infection')return r==='☣️ 감염자'&&!!a.target;if(g==='forbidden')return r==='유도자'&&!!a.target;return false}
function renderActions(){let d=$('privateActions'),m=$('minorActions'),gid=S.game_id,a=S.assignment||{},stage=S.round_result?.stage||'none',sig=JSON.stringify([gid,S.round_no,a.role,a.mission,a.target,stage,!!S.round_result,a.extra?.rerolls_left,a.extra?.ally,a.extra?.betray_used,a.extra?.investigation_used,a.extra?.ghost_used,a.extra?.hidden_bomb,S.game_data_public?.my_submitted,S.game_data_public?.my_guessed]);if(d.dataset.sig===sig)return;d.dataset.sig=sig;let h='';
 if(canReportSuccess()&&gid!=='betray')h=`<button class="btn primary full" onclick="act('mission_success')">성공 신고</button>`;
 if(gid==='oddq'){if(!S.round_result)h=S.game_data_public?.my_submitted?`<div class="notice">선택 완료 · 다른 참가자를 기다리는 중입니다.</div>`:`${peopleSelect('personSel')}<textarea id="reason" maxlength="80" placeholder="이유는 짧게 · 선택사항"></textarea><button class="btn primary full" onclick="act('submit',{target:$('personSel').value,reason:$('reason').value})">선택 완료</button>`;else if(S.round_result.stage==='guess')h=S.game_data_public?.my_guessed?`<div class="notice">추리 완료 · 다른 참가자를 기다리는 중입니다.</div>`:`${peopleSelect('guessSel')}<button class="btn primary full" onclick="act('guess',{target:$('guessSel').value})">이 사람이 다른 질문</button>`;else h=`<div class="notice">정답 공개 중 · 자동으로 다음 라운드로 넘어갑니다.</div>`}
 if(gid==='anon'){if(!S.round_result)h=S.game_data_public?.my_submitted?`<div class="notice">익명 제출 완료 · 다른 참가자를 기다리는 중입니다.</div>`:`${peopleSelect('personSel')}<textarea id="reason" maxlength="80" placeholder="이유 한 줄 · 선택사항"></textarea><button class="btn primary full" onclick="act('submit',{target:$('personSel').value,reason:$('reason').value})">익명 제출</button>`;else h=`<div class="notice">결과 공개 중 · 곧 다음 라운드가 시작됩니다.</div>`}
 if(gid==='choice'&&a.extra){if(!S.round_result)h=S.game_data_public?.my_submitted?`<div class="notice">선택 완료 · 다른 참가자를 기다리는 중입니다.</div>`:`<div class="stack"><button class="btn primary full" onclick="act('submit',{choice:'a'})">${esc(a.extra.a)}</button><button class="btn secondary full" onclick="act('submit',{choice:'b'})">${esc(a.extra.b)}</button></div>`;else h=`<div class="notice">선택 결과 공개 중 · 곧 다음 라운드가 시작됩니다.</div>`}
 if(gid==='betray'){let ally=a.extra?.ally,used=a.extra?.betray_used;h=`<button class="btn primary full" onclick="act('mission_success')">목표 진행 +2</button><div style="height:7px"></div>${ally?`<div class="notice">현재 동맹 · ${esc(nameOf(ally))}</div>${used?'':`<button class="btn danger full" style="margin-top:7px" onclick="act('betray')">동맹 배신 +3</button>`}`:`${peopleSelect('allySel')}<button class="btn secondary full" style="margin-top:7px" onclick="act('alliance',{target:$('allySel').value})">동맹 맺기</button>`}`;}
 if(gid==='night'&&a.role==='조사자'&&!a.extra?.investigation_used)h+=`<div class="divider"></div><div class="sub">조사자 1회 능력</div>${peopleSelect('investigateSel')}<button class="btn secondary full" style="margin-top:7px" onclick="act('night_investigate',{target:$('investigateSel').value})">비밀 조사</button>`;
 if(gid==='bounty'&&a.role==='👻 방해자'&&!a.extra?.ghost_used){let live=S.players.filter(p=>p.active&&p.alive&&p.id!==S.me.id);h=`<div class="notice">탈락했지만 계속 참여합니다. 가짜 경고를 한 번 보낼 수 있습니다.</div><select id="ghostSel">${live.map(p=>`<option value="${p.id}">${esc(p.name)}</option>`).join('')}</select><button class="btn secondary full" style="margin-top:7px" onclick="act('ghost_hint',{target:$('ghostSel').value})">가짜 경고 보내기</button>`}
 if(gid==='night')h+=`<div class="divider"></div>${peopleSelect('nightVote')}<button class="btn ghost full" style="margin-top:7px" onclick="act('night_vote',{target:$('nightVote').value})">현재 의심 투표</button>`;
 d.innerHTML=h;m.innerHTML=canReroll()?`<button class="btn ghost" onclick="act('reroll')">미션 바꾸기 · 1회</button>`:'';
}
function renderResult(){view('result');$('winner').textContent=S.result?.winner||'공동 결과';$('resultTitle').textContent=(S.result?.title||'')+' · 이번 판';let gp=S.result?.game_points||{},wins=new Set(S.result?.winner_ids||[]);$('resultScores').innerHTML=[...S.players].filter(p=>p.active).sort((a,b)=>(Number(wins.has(b.id))-Number(wins.has(a.id)))||((gp[b.id]||0)-(gp[a.id]||0))).map((p,i)=>`<div class="scoreline"><span>${wins.has(p.id)?'✓ ':''}${esc(p.name)} <span class="sub">· 누적 ${p.score}</span></span><b>${(gp[p.id]||0)>=0?'+':''}${gp[p.id]||0}</b></div>`).join('');$('replayBtn').classList.toggle('hidden',!isHost());$('otherBtn').classList.toggle('hidden',!isHost())}
function renderTodayData(){if(!S)return;let t=S.today;$('todayText').innerHTML=`${t.games||0}게임 플레이<br><b>종합 1위 ${esc(t.leader||'-')}</b> · ${t.leader_score||0}점<br>최다 제거 ${esc(t.hunter||'-')} · ${t.hunter_n||0}회<br>미션왕 ${esc(t.missions||'-')} · ${t.missions_n||0}회<br>배신왕 ${esc(t.betrayer||'-')} · ${t.betrayals||0}회`;$('todayScores').innerHTML=[...S.players].filter(p=>p.active).sort((a,b)=>b.score-a.score).map((p,i)=>`<div class="scoreline"><span>${i+1}. ${esc(p.name)}</span><b>${p.score}</b></div>`).join('')}
function showToday(){if(!S)return;returnView=S.phase;manualView='today';view('today');renderTodayData()}
function goCurrent(){manualView=null;render()}

const _v41RenderPlay=renderPlay;
renderPlay=function(){
  _v41RenderPlay();
  let me=S?.me&&S.players.find(p=>p.id===S.me.id);
  if(me&&!me.active){$('role').textContent='이번 판 제외';$('mission').textContent='다음 판부터 다시 참가할 수 있습니다.';$('targetLine').textContent='';$('privateActions').innerHTML='';$('minorActions').innerHTML=''}
  if($('nextRoundBtn')){let ok=isHost()&&['oddq','anon','choice'].includes(S.game_id)&&!!S.round_result&&(S.game_id!=='oddq'||S.round_result.stage==='done');$('nextRoundBtn').classList.toggle('hidden',!ok)}
};
try{document.querySelector('#pendingBox .sub:last-child').textContent='응답이 없으면 확인 요청이 자동 취소됩니다.'}catch{}
if(polling){clearInterval(polling);polling=null} if(displayMode||saved()){refresh(true).then(startPoll)}
</script>
